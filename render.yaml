services:
  - type: web
    name: fullstack
    buildCommand: cd osa2/kokrende && npm install && npm run build && cd ../../osa3/notebackend && npm install
    startCommand: cd osa3/notebackend && npm start
    staticSite: false
    envVars:
      - key: NODE_ENV
        value: production