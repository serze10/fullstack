services:
  - type: web
    name: fullstack
    dir: osa3/notebackend
    buildCommand: npm install && cd ../kokrende && npm install && npm run build && cd ../notebackend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production