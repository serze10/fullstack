services:
  - type: web
    name: fullstack
    buildCommand: cd osa3/notebackend && npm install && cd ../../osa2/kokrende && npm install && npm run build
    startCommand: cd osa3/notebackend && npm start
    envVars:
      - key: NODE_ENV
        value: production